use counting_async::{concurrency, streams};

fn main() {
    // concurrency::counting();
    // concurrency::counting_join();
    // concurrency::channel();
    // concurrency::channel_dyn_futures();
    // concurrency::race();
    // streams::streams();
    streams::messages();
}
